services:
  - type: web
    name: hoodfundbot
    runtime: python
    buildCommand: pip install -r bot/requirements.txt
    startCommand: python bot/main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.9
